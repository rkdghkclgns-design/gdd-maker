{
  "name": "gdd-maker",
  "version": "0.3.0",
  "private": true,
  "description": "GDD 메이커 — 로컬 게임 기획서 빌더 (Tauri 데스크톱 패키징)",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "serve": "python -m http.server 8000 --bind 127.0.0.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.6.0"
  }
}
